name: groviq
description: Groviq - Blinkit style grocery & food delivery Flutter boilerplate
version: 1.0.0+1
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.14.0
  provider: ^6.0.5

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
